# Base URL of the deployed votes Worker (no trailing slash). See /worker.
# Leave unset for local-only mode: each visitor's own vote is remembered, but
# no shared community count is shown.
# VITE_VOTES_API=https://trailfinder-votes.your-subdomain.workers.dev
