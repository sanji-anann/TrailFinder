name = "trailfinder-votes"
main = "index.js"
compatibility_date = "2024-11-01"

# KV namespace that holds the per-trail vote counters.
# Create it once with:  npx wrangler kv namespace create VOTES
# then paste the returned id below (and a preview_id for `wrangler dev`).
kv_namespaces = [
  { binding = "VOTES", id = "37682620ad7544158588a66967b2dd4a" }
]
